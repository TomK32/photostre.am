%ul
  - pages.each do |page|
    %li
      = link_to page.title, page_url(page.permalink)
      - unless page.children.published.empty?
        = render :partial => 'pages/pages', :object => page.children