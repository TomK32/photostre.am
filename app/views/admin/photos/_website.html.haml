%div.droppable[website]
  %span.title= link_to website.title, admin_photos_path({:website_id => website.id})
  %small.count
    = t(:'admin.photos.index.items', :count => website.related_photos.count)
%ul.info.albums
  = render :partial => 'album', :collection => website.albums
