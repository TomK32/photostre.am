= error_messages_for :source

- form_for current_object, {:url => object_path(current_object), :method => :put} do |form|
  = render 'form', :object => form
  
  %p.submit= submit_tag t(:'.save'), :name => nil