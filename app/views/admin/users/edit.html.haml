- content_for :head do
  = stylesheet_link_tag 'admin/forms'

= form_for :user, @user||User.new, :url => resource_path, :html => {:method => :put} do |form|
  = form.error_messages

  = render 'form', :form => form
  %p
    = submit_tag t(:'.submit'), :name => nil
