#related_photos
  - if collection.empty?
    .empty
      = t(:'.empty')
  - else
    - collection.each do |related_photo|
      %div[related_photo]
        = link_to(image_tag(related_photo.photo_url(:square)), resource_path(related_photo), :remote => true)