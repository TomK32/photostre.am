- title(t(:'.page_title'))
- content_for :info do
  = render :partial => 'info'

%h2= link_to t(:'.websites'), admin_websites_path
- if @websites.empty?
  %p.error= t(:'websites.index.no_websites')
  = t(:'websites.index.add_new_website', :link => link_to(t(:'websites.index.new_website'), new_admin_website_path))
- else
  %dl
    - @websites.each do |website|
      %dt[website]{:class => website.active? ? 'active' : 'inactive'}
        = link_to website.domain, 'http://' + website.domain
      %dd
        = t(:".website.#{website.active? ? 'active' : 'inactive'}")
        = link_to t(:'.website.details'), admin_website_path(website)

%h2= link_to t(:'.sources'), admin_sources_path
- if @sources.empty?
  %p.error=  t(:'.no_sources') 
  = t(:'.add_new_source', :link => link_to(t(:'.new_source'), new_admin_source_path))
- else

  %dl#sources
    - @sources.each do |source|
      %dt[source]{:class => source.active? ? 'active' : 'inactive'}
        = link_to source.title, admin_source_path(source)
        = source.source_title
      %dd
        = pluralize(source.photos.count, t(:'.photos'))
        = link_to t(:'.flickr.photostream'), source.photostream_url
        = link_to t(:'.flickr.profile'), source.profile_url


%h2= link_to t(:'.photos'), admin_photos_path
- unless @photos.empty?
  #photos
  - @photos.each do |photo|
    = link_to image_tag(photo.icon_url), admin_photo_path(photo)
