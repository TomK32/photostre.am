%p
  = t(:'.success') 
  = t(:'.information_missing')

= error_messages_for :user
= form_for :user, @user, :url => users_url do |form|
  = render :partial => 'form', :form => form
  %p
    = submit_tag t(:'.create'), :name => nil
