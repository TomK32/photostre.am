- content_for :head do
  = stylesheet_link_tag 'admin/forms'

= form_for :user, @user||User.new, :url => users_url do |form|
  = form.error_messages

  = render 'users/form', :form => form
  %p
    = submit_tag t(:'.create'), :name => nil
