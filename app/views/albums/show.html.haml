%div[resource]
  .description= raw resource.description_html

  #photos
    - @photos.each do |photo|
      .photo= link_to image_tag(photo.photo_url(:square)), album_photo_path(resource.permalink, photo.permalink)