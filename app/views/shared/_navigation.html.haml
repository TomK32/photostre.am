%ul.pages
  - current_website.pages.published.roots.each do |page|
    %li= link_to page.title, page_path(page.permalink)
%ul.albums
  - current_website.albums.published.roots.each do |album|
    %li= link_to album.title, album_path(album.permalink)
