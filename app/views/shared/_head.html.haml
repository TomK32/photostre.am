%title= page_title

- [:meta_geourl, :meta_keywords, :meta_description].each do |meta|
  - unless(yield(meta).blank?)
    %meta{ :name => meta, :content => yield(meta) }
= stylesheet_link_tag %w(layout application forms colors photos), :cache => 'cache/all.js'