#album
  %h1= resource.title
  .description= raw resource.description_html

  #photos
    - @photos.each do |photo|
      .photo= link_to image_tag(photo.photo_url(:medium)), album_photo_path(resource.permalink, photo.permalink)